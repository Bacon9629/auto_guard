<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/camera_menu"
        android:icon="@drawable/face"
        android:title="@string/admin_list"
        android:onClick="face_list"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/build_white_list"
        android:icon="@drawable/face"
        android:title="@string/custom_list"
        android:onClick="face_list"
        app:showAsAction="never"
        />

    <item
        android:id="@+id/menu_snapshot_list"
        android:title="@string/snapshot_list"
        app:showAsAction="never"
        android:onClick="face_list"
        />

</menu>