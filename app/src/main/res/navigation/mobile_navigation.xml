<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_robot"
        android:name="com.bacon.auto_guard.ui.robot.RobotFragment"
        android:label="@string/title_1"
        tools:layout="@layout/fragment_layout_robot" >
        <action
            android:id="@+id/action_navigation_robot_to_navigation_home"
            app:destination="@id/navigation_home" />
    </fragment>

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.bacon.auto_guard.ui.home.HomeFragment"
        android:label="@string/title_2"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_navigation_home_to_navigation_notifications"
            app:destination="@id/navigation_notifications" />
    </fragment>

    <fragment
        android:id="@+id/navigation_notifications"
        android:name="com.bacon.auto_guard.ui.notifications.NotificationsFragment"
        android:label="@string/title_3"
        tools:layout="@layout/fragment_notifications" >
        <action
            android:id="@+id/action_navigation_notifications_to_navigation_robot"
            app:destination="@id/navigation_robot" />
    </fragment>
</navigation>